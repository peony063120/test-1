import { Box, Button, Paper, Stack, TextField, Typography } from '@mui/material'; import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'; import { systemSettingApi } from '@/api/endpoints/system-setting.api'; import toast from 'react-hot-toast';
const SystemSettings = () => { const client = useQuery({ queryKey: ['system-settings'], queryFn: systemSettingApi.list }); const queryClient = useQueryClient(); const update = useMutation({ mutationFn: ({ key, value }: { key: string; value: string }) => systemSettingApi.update(key, value), onSuccess: () => queryClient.invalidateQueries({ queryKey: ['system-settings'] }) }); return <Box><Typography variant="h4" gutterBottom>Cài đặt hệ thống</Typography><Stack spacing={2}>{(client.data || []).map((setting) => <Paper key={setting.key} sx={{ p: 2 }}><TextField label={setting.key} defaultValue={setting.value} fullWidth onBlur={(event) => { if (event.target.value !== setting.value) update.mutate({ key: setting.key, value: event.target.value }, { onSuccess: () => toast.success('Đã lưu cài đặt.') }); }} /></Paper>)}</Stack></Box>; };
export default SystemSettings;
