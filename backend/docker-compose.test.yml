version: '3.9'
services:
  postgres-test:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: product_management_test
    ports:
      - '5433:5432'
  redis-test:
    image: redis:7-alpine
    ports:
      - '6380:6379'
